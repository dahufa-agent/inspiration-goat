version: "1"
services:
  - type: web
    name: test-server
    env: node
    region: singapore
    buildCommand: echo "No build needed"
    startCommand: node test-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
