services:
  - type: web
    name: inspiration-goat-server
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: COZE_API_TOKEN
        sync: false
      - key: DATABASE_URL
        sync: false
