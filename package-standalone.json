{
  "name": "inspiration-goat-standalone",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "install": "cd server && npm install",
    "build": "cd server && npm run build",
    "start": "cd server && node dist/index.js"
  }
}
