version: "1"
services:
  - type: web
    name: inspiration-goat-api
    env: node
    buildCommand: cd server && npm install && npm run build
    startCommand: cd server && PORT=5001 npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
