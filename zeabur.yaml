# 灵感山羊 - Zeabur部署配置

services:
  goat-api:
    type: docker
    dockerfilePath: ./Dockerfile
    context: .
    env:
      - name: NODE_ENV
        value: production
      - name: PORT
        value: "8080"
